function showCart() {
    const cartBody = document.getElementById("cart-body");

    const cartItems = Cookies.get('items');
    const cartItemList = currentItems.split("|");

    if (itemsList == undefined) {
        return;
    }

}
